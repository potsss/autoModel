当前的方式是：

1、对所有的字段进行翻译
2、对于主表内的字段进行信息的相关性判断，筛选部分字段
3、对于外表的字段，让LLM进行特征创造
4、演化过程中的时候，会设置基因长度的上下限
5、会增加对抗性的参数强度，包括经济性和因果性

1、字段的描述，表之间的关系，能否有 能否有标准格式，质量怎么样
2、因果性和经济性是不是需要 不好量化，所以后面再看是否需要
3、需不需要提前筛选 暂时认为需要，最后得到的数量也是很少的特征，这个是好事
4、多表的实验 主要是看跨表的特征聚合